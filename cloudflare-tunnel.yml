tunnel: syntera-agent-service
credentials-file: .cloudflare/credentials.json

ingress:
  # Webhook endpoint for LiveKit Egress
  - hostname: syntera-agent-service.your-domain.com
    service: http://localhost:4002
  # Catch-all rule (must be last)
  - service: http_status:404



